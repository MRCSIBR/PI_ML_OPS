version: "3.8"

services:
  steam-mlops-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: steam_mlops_api
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped
